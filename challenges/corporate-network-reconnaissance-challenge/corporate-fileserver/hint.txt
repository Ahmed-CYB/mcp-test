Network Reconnaissance Crypto Challenge Hints:

1. Password Decryption Chain:
   - First decode the base64 strings
   - Then apply ROT13 decryption
   - Results are password hashes that spell words

2. Fragment Assembly:
   - Fragment 1: Base64 encoded in auth.log
   - Fragment 2: Base64 encoded in database backup
   - Fragment 3: XOR encrypted in partition file

3. XOR Decryption:
   - Use the decrypted file_manager password as XOR key
   - Convert hex to bytes, XOR with key, convert back

4. Tools needed:
   - Base64 decoder
   - ROT13 cipher
   - XOR calculator
   - Hex converter

5. Flag format: CTF{corporate_network_security}
   Combine all three fragments in order