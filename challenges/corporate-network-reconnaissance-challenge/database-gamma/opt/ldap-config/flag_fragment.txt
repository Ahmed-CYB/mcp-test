# TechCorp LDAP Configuration Fragment
# This file contains the middle portion of the corporate flag
# Must be combined with fragments from other network machines

# Flag fragment from database-gamma: n3tw0rk_r3c0n_
# Instructions: Combine all three fragments in order:
# webserver-alpha + database-gamma + fileserver-beta

# LDAP sync credentials for staging:
dn: cn=ldap_sync,dc=techcorp,dc=local
objectClass: person
cn: ldap_sync
sn: Service Account
userPassword: sync_pass
description: LDAP synchronization service account

# Development LDAP entries:
dn: ou=users,dc=techcorp,dc=local
objectClass: organizationalUnit
ou: users

dn: cn=staging_user,ou=users,dc=techcorp,dc=local
objectClass: person
cn: staging_user
sn: Staging User
userPassword: staging123
department: Development