version: '3.8'
services:
  victim:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctf-reflected-xss-shop-victim
    hostname: victim
    networks:
      ctf-network:
        ipv4_address: 172.22.193.10
  attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile.attacker
    container_name: ctf-reflected-xss-shop-attacker
    hostname: attacker
    networks:
      ctf-network:
        ipv4_address: 172.22.193.100
networks:
  ctf-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.22.193.0/24
          gateway: 172.22.193.1
