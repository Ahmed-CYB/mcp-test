version: '3.8'

services:
  ctf-corporate-data-breach-ftp-server-misconfiguration-attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile.attacker
    container_name: ctf-corporate-data-breach-ftp-server-misconfiguration-attacker
    hostname: security-auditor
    labels:
      com.ctf.machine.type: "attacker"
      com.ctf.challenge: "corporate-data-breach-ftp-server-misconfiguration"
    networks:
      ctf-corporate-data-breach-ftp-server-misconfiguration-net:
        ipv4_address: 172.25.193.3
      ctf-instances-network:
    stdin_open: true
    tty: true

  ctf-corporate-data-breach-ftp-server-misconfiguration-corporate-fileserver:
    build:
      context: ./corporate-fileserver
      dockerfile: Dockerfile
    container_name: ctf-corporate-data-breach-ftp-server-misconfiguration-corporate-fileserver
    hostname: corporate-fileserver
    labels:
      com.ctf.machine.type: "victim"
      com.ctf.challenge: "corporate-data-breach-ftp-server-misconfiguration"
    networks:
      ctf-corporate-data-breach-ftp-server-misconfiguration-net:
        ipv4_address: 172.25.193.12
    ports:
      - "21"
    stdin_open: true
    tty: true

networks:
  ctf-corporate-data-breach-ftp-server-misconfiguration-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.193.0/24
          gateway: 172.25.193.1
  ctf-instances-network:
    external: true
