version: '3.8'

services:
  ctf-ftp-server-infiltration-ftp-server:
    build:
      context: ./ftp-server
      dockerfile: Dockerfile
    container_name: ctf-ftp-server-infiltration-ftp-server
    hostname: ftp-server
    labels:
      com.ctf.machine.type: "victim"
      com.ctf.challenge: "ftp-server-infiltration"
    networks:
      ctf-ftp-server-infiltration-net:
        ipv4_address: 172.23.198.71
    ports:
      - "21"
      - "22"
    stdin_open: true
    tty: true

  ctf-ftp-server-infiltration-attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile.attacker
    container_name: ctf-ftp-server-infiltration-attacker
    hostname: attacker
    labels:
      com.ctf.machine.type: "attacker"
      com.ctf.challenge: "ftp-server-infiltration"
    networks:
      ctf-ftp-server-infiltration-net:
        ipv4_address: 172.23.198.3
      ctf-instances-network:
    stdin_open: true
    tty: true

networks:
  ctf-ftp-server-infiltration-net:
    driver: bridge
    ipam:
      config:
        - subnet: [object Object]
  ctf-instances-network:
    external: true
          gateway: 172.23.198.1
  ctf-instances-network:
    external: true
