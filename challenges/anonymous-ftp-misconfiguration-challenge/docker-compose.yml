version: '3.8'

services:
  ctf-anonymous-ftp-misconfiguration-challenge-ftp-server:
    build:
      context: ./ftp-server
      dockerfile: Dockerfile
    container_name: ctf-anonymous-ftp-misconfiguration-challenge-ftp-server
    hostname: ftp-server
    labels:
      com.ctf.machine.type: "victim"
      com.ctf.challenge: "anonymous-ftp-misconfiguration-challenge"
    networks:
      ctf-anonymous-ftp-misconfiguration-challenge-net:
        ipv4_address: 172.23.200.85
    ports:
      - "21"
      - "22"
    stdin_open: true
    tty: true

  ctf-anonymous-ftp-misconfiguration-challenge-attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile.attacker
    container_name: ctf-anonymous-ftp-misconfiguration-challenge-attacker
    hostname: attacker-machine
    labels:
      com.ctf.machine.type: "attacker"
      com.ctf.challenge: "anonymous-ftp-misconfiguration-challenge"
    networks:
      ctf-anonymous-ftp-misconfiguration-challenge-net:
        ipv4_address: 172.23.200.3
      ctf-instances-network:
    stdin_open: true
    tty: true

networks:
  ctf-anonymous-ftp-misconfiguration-challenge-net:
    driver: bridge
    ipam:
      config:
        - subnet: [object Object]
  ctf-instances-network:
    external: true
          gateway: 172.23.200.1
  ctf-instances-network:
    external: true
