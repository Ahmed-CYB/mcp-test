services:
  samba-server:
    build:
      context: ./samba-server
      dockerfile: Dockerfile
    container_name: ctf-samba-guest-access-challenge-samba-server
    networks:
      ctf-samba-guest-access-challenge-net:
        ipv4_address: 172.26.145.63
    restart: unless-stopped
  attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile
    container_name: ctf-samba-guest-access-challenge-attacker
    networks:
      ctf-samba-guest-access-challenge-net:
        ipv4_address: 172.26.145.3
    cap_add:
      - NET_RAW
      - NET_ADMIN
    stdin_open: true
    tty: true
    restart: unless-stopped
networks:
  ctf-samba-guest-access-challenge-net:
    name: ctf-samba-guest-access-challenge-net
    driver: bridge
    internal: false
    ipam:
      config:
        - subnet: 172.26.145.0/24
          gateway: 172.26.145.1
