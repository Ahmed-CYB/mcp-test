# /etc/default/tftpd-hpa

TFTP_USERNAME="tftp"
TFTP_DIRECTORY="/srv/tftp"
TFTP_ADDRESS="0.0.0.0:26"
TFTP_OPTIONS="--secure --create --permissive"

# Misconfiguration: --create and --permissive flags allow file uploads
# and modification without proper access controls