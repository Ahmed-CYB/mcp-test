version: '3.8'
services:
  victim:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctf-ftp-weak-password-victim
    hostname: victim
    networks:
      ctf-network:
        ipv4_address: 172.23.194.10
  attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile.attacker
    container_name: ctf-ftp-weak-password-attacker
    hostname: attacker
    networks:
      ctf-network:
        ipv4_address: 172.23.194.100
      ctf-instances-network: {}
networks:
  ctf-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.23.194.0/24
          gateway: 172.23.194.1
  ctf-instances-network:
    external: true
    name: ctf-instances-network
