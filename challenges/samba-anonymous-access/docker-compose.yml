services:
  samba-server:
    build:
      context: ./samba-server
      dockerfile: Dockerfile
    container_name: ctf-samba-anonymous-access-samba-server
    networks:
      ctf-samba-anonymous-access-net:
        ipv4_address: 172.23.145.31
    restart: unless-stopped
  attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile
    container_name: ctf-samba-anonymous-access-attacker
    networks:
      ctf-samba-anonymous-access-net:
        ipv4_address: 172.23.145.3
    cap_add:
      - NET_RAW
      - NET_ADMIN
    stdin_open: true
    tty: true
    restart: unless-stopped
networks:
  ctf-samba-anonymous-access-net:
    name: ctf-samba-anonymous-access-net
    driver: bridge
    internal: false
    ipam:
      config:
        - subnet: 172.23.145.0/24
          gateway: 172.23.145.1
