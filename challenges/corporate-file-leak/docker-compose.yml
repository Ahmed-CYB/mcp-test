services:
  fileserver:
    build:
      context: ./fileserver
      dockerfile: Dockerfile
    container_name: ctf-corporate-file-leak-fileserver
    networks:
      ctf-corporate-file-leak-net:
        ipv4_address: 172.25.194.198
    restart: unless-stopped
  attacker:
    build:
      context: ./attacker
      dockerfile: Dockerfile
    container_name: ctf-corporate-file-leak-attacker
    networks:
      ctf-corporate-file-leak-net:
        ipv4_address: 172.25.194.3
      ctf-instances-network: {}
    cap_add:
      - NET_RAW
      - NET_ADMIN
    stdin_open: true
    tty: true
    restart: unless-stopped
networks:
  ctf-corporate-file-leak-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.194.0/24
          gateway: 172.25.194.1
  ctf-instances-network:
    external: true
